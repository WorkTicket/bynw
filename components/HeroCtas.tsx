import PrimaryCTA from "@/components/PrimaryCTA"

/** Hero CTA — scroll to collections. */
export default function HeroCtas() {
  return (
    <div className="mt-6 sm:mt-8">
      <PrimaryCTA href="/#colecciones" size="lg">
        Ver colecciones
      </PrimaryCTA>
    </div>
  )
}
